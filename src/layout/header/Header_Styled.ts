import styled from "styled-components";

const Header = styled.header`
    position: fixed; 
    top: 0;
    right: 0;
    left: 0;      
    z-index: 99999;
    padding-top: 30px;
`;

export const S = {
    Header
}